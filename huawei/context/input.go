package context

/**
_, err := fmt.Scan(&input)

_, err := fmt.Scanf("%d %d", &num1, &num2)

reader := bufio.NewReader(os.Stdin)
input, _ := reader.ReadString('\n')
numStrArr := strings.Fields(input)
*/

/**
slice
map
byte
for
channel
fmt 的打印格式化函数

快排
*/
